# Changelog: May 2, 2026 - Nashville Authority Hardening & Security

## SEO & Indexing Infrastructure
- **Crawl-Force Deployment**: Implemented the "Nashville Field Operations" section on the homepage with high-authority editorial links to Franklin, Belle Meade, and Limestone hubs.
- **Visual E-E-A-T**: Generated and integrated three documentary-style, hyper-realistic project images to prove real-time field activity to Googlebot.
- **Canonical Recovery**: Fixed a critical bug where the root layout was overriding all child pages with a homepage canonical. Every page now has its own unique, self-referencing canonical.
- **Schema Breadcrumbs**: Injected `BreadcrumbList` JSON-LD schema into the global Navigation/Breadcrumb system for rich search result snippets.
- **Entity Unification**: Standardized the business `@id` to `https://nashvillepoolremoval.com/#organization` across the entire graph.
- **Robots.txt Hardening**: Disallowed internal `/_next/` and `/api/` paths to optimize crawl budget focus.

## Security & Lead Integrity
- **HoneyPot Defense**: Implemented hidden spam traps on the Nashville Calculator and Contact forms to block automated lead-scrapers.
- **Strict CSP**: Deployed a Content Security Policy in `next.config.mjs` to block XSS and unauthorized data exfiltration.
- **Credential Isolation**: Overhauled GSC automation scripts to use environment variables for credential paths, removing hardcoded file system absolute paths.
- **Lead Attribution**: Corrected the Supabase routing to use the `emd_leads_nashville` table for market-specific data isolation.

## Technical Fixes
- **Build Resolution**: Fixed a syntax error in the Breadcrumb component caused by an unclosed React Fragment.
- **Component Refactor**: Transitioned `Services`, `About`, and `Cost` pages into Server Components to enable unique SEO metadata exports.

## Strategic Expectations (Post-Deployment)
- **Indexing Hold Release (72–96 Hours)**: The "Canonical Recovery" fix is the single most important correction. We expect the majority of the "Neutral" location spokes to transition to "Indexed" status once Googlebot processes the unique canonicals and the Indexing API pings.
- **Rich Result Emergence (7–14 Days)**: With the injection of `BreadcrumbList` schema, we expect to see professional breadcrumb trails appearing in the Google Search results, which will significantly increase Click-Through Rate (CTR).
- **QDF Ranking Spike**: The "Crawl-Force" homepage update (May 2026 Operations) signals high freshness (Query Deserves Freshness). We expect to maintain the **Top 3** position for "pool demolition" and see movement into the **Top 5** for "limestone excavation."
- **Spam Suppression**: The HoneyPot implementation should result in a near-total elimination of automated spam submissions in the `emd_leads_nashville` table.
- **Entity Consolidation**: The unification of the `@id` schema will solidify the "Nashville Pool Professionals" brand as a singular, authoritative organization in the Knowledge Graph, rather than a collection of disconnected pages.
